<div class="col-md-4">
    Artists list
    <ul>
        {{#each model as |artist|}}
            <li>lolol: {{artist.name}}</li>
        {{/each}}
    </ul>
</div>

<div class="col-md-8">
    {{outlet}}
</div>
